{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Pico Build (Docker)",
            "type": "node",
            "request": "launch",
            "preLaunchTask": "docker-pico-build", // Call the build task from tasks.json
            "program": "/bin/echo",   // Dummy program
            "args": [],
            "cwd": "${workspaceFolder}/${input:projectDir}",  // Working directory
        }
    ],
    "inputs": [
        {
            "id": "projectDir",
            "type": "promptString",
            "description": "Enter the project directory (e.g., blink or my-project)"
        }
    ]
}


